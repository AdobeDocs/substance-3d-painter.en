---
title: "UV Tiles | Substance 3D Painter"
description: "Painter > Features > UV Tiles"
---

# UV Tiles

![](banner-uvtiles.jpg)

UV Tiles are a way to texture more than one set of textures, across several UV ranges, inside a Texture Set.

By default with a traditional workflow, a texture is repeated over each UV range. With UV Tiles, each range becomes a dedicated texture instead. This workflow allows to virtually increase the general texture resolution by splitting the UVs into multiple set of textures. UV Tiles currently only support the UDIM naming convention.

To learn more about the UV Tile workflow, see the following pages:

* [Project creation](../../getting-started/project-creation/project-creation.md) with the UV Tile workflow.
* Viewing UV Tiles in the [2D view](../../interface/viewport/2d-view/2d-view.md).
* [UV Tile mask](../../interface/layer-stack/geometry-mask/geometry-mask.md) to improve performances.
* Importing and using [image sequences](image-sequence/image-sequence.md).
* Adjusting the resolution per UV Tiles in the [Texture Set settings](../../interface/texture-set/texture-set-settings/texture-set-settings.md).

>[!NOTE]
>
> Working with UV Tile projects can be demanding, we recommend using an SSD to improve loading times and general performance. We also recommend setting the cache location on an SSD as well.
